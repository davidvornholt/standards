{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["biome.jsonc", "biome.base.jsonc"],
  "tasks": {
    "lint": {
      "outputs": []
    },
    "lint:fix": {
      "cache": false,
      "outputs": []
    },
    "check-types": {
      "outputs": []
    },
    "test": {
      "outputs": [],
      "passThroughEnv": ["GH_TOKEN", "GITHUB_TOKEN"]
    },
    "@davidvornholt/standards#test": {
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/**"],
      "outputs": [],
      "passThroughEnv": ["GH_TOKEN", "GITHUB_TOKEN"]
    }
  }
}
