Second (GPT-5.5 xhigh) and third (Claude Fable 5 high) review loop.

# Review loop: current diff

## Loop segment 1 (passes 1–3) — GPT-5.5 xhigh

- 2026-06-12 13:07:04 +0200 — Scope: user requested a review loop against the current uncommitted diff. Starting diff contains broad web app, admin, shared service, database package, UI package, Biome config, Playwright/Axe a11y, docs, scripts, and secret example changes. Validation gate command: `bun run check:fix`.
- 2026-06-12 13:07:04 +0200 — Skill routing: inspected local skill description frontmatter; applying `review-loop`, `review`, and database rules for touched database package/schema/config files.
- 2026-06-12 13:08:02 +0200 — Gate attempt 1: `bun run check:fix` failed in `@fesk/web#test:a11y`. Lint fixes, type checks, unit tests, and build completed; Playwright/Axe tests failed because `assertA11yDatabaseSeedingAllowed` rejected the currently loaded `DATABASE_URL` database name as non-disposable.
- 2026-06-12 13:09:24 +0200 — Gate attempt 2: `DATABASE_URL=postgresql://fesk_web:fesk_web@127.0.0.1:5432/fesk_web_dev bun run check:fix` got past the a11y database guard, but `@fesk/web#test:a11y` failed because no PostgreSQL server was reachable at `127.0.0.1:5432`; DB-backed routes rendered errors, causing 67 a11y failures/timeouts.
- 2026-06-12 13:10:48 +0200 — Environment setup: started temporary Podman PostgreSQL container `fesk-a11y-postgres` with disposable database `fesk_web_dev`; ran `DATABASE_URL=postgresql://fesk_web:fesk_web@127.0.0.1:5432/fesk_web_dev bun run --cwd packages/db db:migrate`, and migrations applied successfully.
- 2026-06-12 13:11:30 +0200 — Gate attempt 3: `DATABASE_URL=postgresql://fesk_web:fesk_web@127.0.0.1:5432/fesk_web_dev bun run check:fix` passed. Result: 17/17 Turbo tasks successful, including lint fixes, type checks, unit tests, build, and 110 Playwright/Axe checks.
- 2026-06-12 13:12:24 +0200 — Pass 1 started. Delta reviewed: full current dirty worktree, including tracked diffs and untracked files. Fan-out reviewers invoked: security, accessibility/frontend UX, architecture/types/database, tests/docs/config, and catch-all. Retry count: 0 for all initial invocations. Reviewers were instructed not to edit files or rerun the full gate.
- 2026-06-12 13:25:16 +0200 — Pass 1 findings resolved: forwarded `FormControl` props to cloned children, restored desktop nav focus on Escape, added a birthday picker a11y interaction test, made rich-text link dialog validation live with `role="alert"`, changed `@fesk/web` `test:a11y` back to `playwright test`, provisioned PostgreSQL/Chromium/migrations for CI check, updated touched GitHub Actions to current stable majors, documented expanded a11y coverage and seeding requirements, added Playwright config CI behavior tests, moved role existence checks after user-update permission checks, and validated role permission uniqueness before role row updates.
- 2026-06-12 13:25:16 +0200 — Pass 1 discarded: sibling-feature import in `apps/web/src/features/welcome/presentation/components/landing/news-section.tsx` predates the current diff behavior and would require a broader feature-boundary relocation; left documented as pre-existing/out of scope for this loop.
- 2026-06-12 13:25:16 +0200 — Gate attempts 4-5 after fixes: attempt 4 failed on new birthday-picker test lint (`useTopLevelRegex`, false-positive `noSecrets` selector); attempt 5 failed on a real Drizzle readonly-array type in `role-write-effects.ts` and stale partial `.next/types` output from an interrupted build. Both were corrected. Attempt 6, `rm -rf apps/web/.next && A11Y_DATABASE_SEEDING=1 DATABASE_URL=postgresql://fesk_web:fesk_web@127.0.0.1:5432/fesk_web_dev bun run check:fix`, passed: 17/17 Turbo tasks successful, including 124 web unit tests and 112 Playwright/Axe checks.
- 2026-06-12 13:36:42 +0200 — Pass 2 started after gate attempt 6. Delta reviewed: pass-1 fixes with full current diff as context. Reviewer invocations: accessibility/frontend UX, tests/docs/config, catch-all. Retry count: 0.
- 2026-06-12 13:36:42 +0200 — Pass 2 findings fixed: added desktop nav pointer-leave close plus a hover-leave Playwright state; added a visible rich-text editor focus ring and Playwright focus assertion; made the rich-text link dialog footer responsive on narrow screens; expanded the link dialog a11y test to cover invalid URL error state and aria wiring; documented root a11y gate database/seeding prerequisites in `README.md` and `AGENTS.md`; strengthened the role-update duplicate-permission test to prove the failure happens after authorization but before database/audit boundaries.
- 2026-06-12 13:36:42 +0200 — Gate attempts 7-10 after pass 2 fixes: attempt 7 failed on hover-test magic numbers; attempt 8 failed because the role-ordering test carried an unprovided DB service type; attempt 9 failed because the fake DB used the tag type rather than the Drizzle service type; all were corrected. Attempt 10, `rm -rf apps/web/.next && A11Y_DATABASE_SEEDING=1 DATABASE_URL=postgresql://fesk_web:fesk_web@127.0.0.1:5432/fesk_web_dev bun run check:fix`, passed: 17/17 Turbo tasks successful, including 125 web unit tests and 114 Playwright/Axe checks.
- 2026-06-12 13:41:29 +0200 — Pass 3 started after gate attempt 10. Delta reviewed: pass-2 fixes with full current diff as context. Reviewer invocations: one unscoped convergence reviewer. Retry count: 0.
- 2026-06-12 13:41:29 +0200 — Pass 3 result: clean. No blocking findings, non-blocking findings, or nits. Reviewer did not rerun the full gate; focused checks included diff whitespace, conflict/stale-reference sweeps, line-count exception sweep, and a small Playwright probe against the built app.
- 2026-06-12 13:42:32 +0200 — Final workflow verification: downloaded `actionlint` 1.7.12 with the same temporary flow used by CI and ran it successfully against `.github/workflows/ci.yml`.

## Loop segment 2 (passes 4–8) — Claude Fable 5 high

- 2026-06-12 14:15:26 +0200 — New loop invocation: user requested a fresh review loop against the current diff. No worktree files changed since the previous loop converged at 13:41:29 +0200; the diff under review is identical to the one that passed gate attempt 10 and pass 3. Treating this as a fresh pass 4 with fresh-eyes reviewers over the full current diff.
- 2026-06-12 14:18:40 +0200 — Environment re-provisioning: the temporary PostgreSQL container from the earlier loop was gone; recreated `fesk-a11y-postgres` (postgres:17-alpine, disposable db `fesk_web_dev`) and applied migrations successfully. Gate re-run started: `A11Y_DATABASE_SEEDING=1 DATABASE_URL=postgresql://fesk_web:fesk_web@127.0.0.1:5432/fesk_web_dev bun run check:fix`.
- 2026-06-12 14:22:10 +0200 — Gate attempt 11 (re-run for new loop): `A11Y_DATABASE_SEEDING=1 DATABASE_URL=... bun run check:fix` passed. 17/17 Turbo tasks successful (11 cached), 114 Playwright/Axe checks passed. No worktree changes produced by lint:fix.
- 2026-06-12 14:22:10 +0200 — Pass 4 started. Delta reviewed: full current dirty worktree (identical to pass-3 state), fresh-eyes fan-out. Reviewer lenses invoked: security, accessibility/frontend UX, architecture/types/database, tests/docs/config, catch-all. Each receives the full current diff as context. Reviewers instructed not to edit files and not to re-run the full gate.
- 2026-06-12 14:42:00 +0200 — Pass 4 reviewer invocations complete: security (1), accessibility/frontend UX (1 orchestrating agent + 3 sub-reviewers it spawned), architecture/types/database (1), tests/docs/config (1), catch-all (1). Retries: 0. Findings synthesized and validated below.
- 2026-06-12 14:42:00 +0200 — P4-F01 security non-blocking VALID (pre-existing, fixing): `sanitizeReturnTo` in apps/web/src/shared/auth/services/entra.ts:34 rejects `//` but not `/\`; WHATWG URL treats `\` as `/`, so `returnTo=/\evil.com` becomes a post-login open redirect to evil.com. Fix: reject backslashes; add tests.
- 2026-06-12 14:42:00 +0200 — P4-F02 tests/config non-blocking VALID (fixing): a11y database guard regex `(?:^|_)(?:a11y|dev|test)$` also accepts bare `dev`/`test`/`a11y` database names, contradicting its own error message and README. Fix: tighten to `_`-suffixed names only; pin with tests.
- 2026-06-12 14:42:00 +0200 — P4-F03 docs non-blocking VALID (fixing): no documented way to provision the local disposable a11y PostgreSQL database; README references a connection string whose role/db only exist in CI. Fix: add a podman-based setup snippet to apps/web/README.md.
- 2026-06-12 14:42:00 +0200 — P4-F04 a11y blocking VALID (fixing): apply-steps.tsx:51 renders a second `<h1>` (page h1 comes from EditorialPageHero in apply-screen). Fix: h1 -> h2.
- 2026-06-12 14:42:00 +0200 — P4-F05 a11y non-blocking VALID (fixing): TextInputField indicates required only via leading `*` in the label while RadioInputField sets aria-required. Fix: add aria-required to the Input.
- 2026-06-12 14:42:00 +0200 — P4-F06 UX/copy VALID (fixing, diff-introduced): reviews.tsx still shows "Tippen Sie auf eine Bewertung, um die Bewegung zu pausieren!" but ReviewMovingCards was rewritten in this diff as a static grid with no motion. Fix: remove stale tip; rename component/file to review-grid for accuracy.
- 2026-06-12 14:42:00 +0200 — P4-F07 catch-all non-blocking VALID (pre-existing, fixing): article-editor-controller.ts:74-76 `setForm(nextSavedForm)` after `await updateArticle` discards edits typed during a pending save. Fix: functional update keeping user edits when state changed during save; extract pure resolver into article-editor-state.ts with test.
- 2026-06-12 14:42:00 +0200 — P4-F08 catch-all non-blocking VALID (pre-existing, fixing): role-write-effects.ts:51 `onConflictDoNothing` means a kept permission's changed scope is never persisted. Fix: `onConflictDoUpdate` on scope.
- 2026-06-12 14:42:00 +0200 — P4-F09 catch-all non-blocking VALID (pre-existing, fixing): article gallery replacement (delete + per-row insert loop) and article create (insert + gallery loop) are not transactional; mid-loop failure loses gallery rows. Fix: add `withTransaction` helper to @fesk/db/runtime (drizzle adapter routes through @effect/sql SqlClient, confirmed in @effect/sql-drizzle internals), batch the gallery insert, wrap create/update mutations.
- 2026-06-12 14:42:00 +0200 — P4-F10 catch-all non-blocking VALID (pre-existing, fixing): deleteUserRecord writes a success audit row before deleting; a failed delete leaves a false audit trail. Fix: wrap audit + delete in one transaction (audit first so self-deletion keeps FK valid; audit_logs.actor_user_id is ON DELETE SET NULL); drop redundant userRoles/sessions deletes (FKs cascade).
- 2026-06-12 14:42:00 +0200 — P4-F11 catch-all non-blocking VALID (pre-existing, fixing): runSops in scripts/dev-env/secrets-document.ts crashes with TypeError (`result.stderr.trim()` on null) when neither sops nor nix can spawn. Fix: null-safe error message using result.error.
- 2026-06-12 14:42:00 +0200 — P4-F12..F15 nits VALID (fixing): identity `Effect.map` in media-upload-queue-upload.ts:41; carousel `reInit` listener never removed in use-carousel-state.ts cleanup; dead `!fieldContext` guard after use in form-field-context.ts (context defaults are truthy casts); pricing create effects skip the audit silently when insert returns no row (now Effect.dieMessage to match article/user convention).
- 2026-06-12 14:42:00 +0200 — P4-F16 nit VALID (fixing): @next/env pinned exact 16.2.6 while next is ^16.2.6 — will drift on patch bumps; align via bun add. P4-F17 nit VALID (fixing): port 3100 duplicated between playwright.config.ts and a11y/admin-fixtures.ts; extract shared a11y/test-base-url.ts and derive HOSTNAME/PORT from it.
- 2026-06-12 14:42:00 +0200 — DISCARDED P4-D01: "EmptyState/PageHeading/Eyebrow muted->foreground are blocking contrast regressions" — false; the change increases contrast (foreground is darker than muted-foreground), is part of a deliberate ink-ramp darkening in theme.css in this diff, and Axe passes. Design-hierarchy choice, not a WCAG issue.
- 2026-06-12 14:42:00 +0200 — DISCARDED P4-D02: "imprint/commitment/pedagogical/data-protection screens missing h1" — false; EditorialPageHero renders the h1 (packages/ui/src/editorial-page-hero.tsx:31). "h2->h3 skip" claim is also wrong: h2 followed by h3 is a correct descent.
- 2026-06-12 14:42:00 +0200 — DISCARDED P4-D03: badge `focus:` vs `focus-visible:` — Badge renders a non-interactive div; focus styles are inert. No impact.
- 2026-06-12 14:42:00 +0200 — DISCARDED P4-D04: carousel prefers-reduced-motion — speculative; scrolling is user-initiated via Embla, no autoplay in the diff.
- 2026-06-12 14:42:00 +0200 — DISCARDED P4-D05: autocomplete tokens on apply form (WCAG 1.3.5) — fields collect data about the student and two guardians, not unambiguously about the user filling the form; wrong tokens cause harmful misfill. Needs product clarification before applying; recorded as needs-clarification rather than fixed.
- 2026-06-12 14:42:00 +0200 — DISCARDED P4-D06: "bare aria-hidden at landing-stages.tsx" — false; the attribute is already `aria-hidden={true}`. DISCARDED P4-D07: className reordering concern — Tailwind class order in markup has no specificity effect; non-issue.
- 2026-06-12 14:42:00 +0200 — DISCARDED P4-D08: function declarations vs arrow consts in new components — AGENTS.md states a soft preference; the codebase has a mixed convention (43 `export function` vs 72 arrow files) and the new files match their local neighborhood. Churn without benefit.
- 2026-06-12 14:42:00 +0200 — DISCARDED P4-D09: pricing-effects factory dedupe — a type-safe factory over three different Drizzle tables and Schema pairs needs ~6 generic parameters or casts, trading ~90 net lines for materially less readable code; files are <115 lines, mechanical, and independently testable. Keeping duplication deliberately; audit-skip inconsistency inside them IS fixed (P4-F15).
- 2026-06-12 14:42:00 +0200 — DISCARDED P4-D10: `rm -rf .next` in build discards build cache — deliberate: stale partial `.next` output broke type checking earlier in this loop (gate attempt 5); correctness over speed. DISCARDED P4-D11: unused public subpaths badge-variants/button-variants/carousel-api — deliberate per the biome.jsonc override comment. DISCARDED P4-D12: review pause control (WCAG 2.2.2) — moot; the moving cards no longer move (see P4-F06).
- 2026-06-12 14:47:06 +0200 — Pass 4 fixes implemented. P4-F01: sanitizeReturnTo now rejects any backslash (entra.ts) with new tests for `/\evil.com`, `/\/evil.com`, `/admin\..\evil`. P4-F02: a11y database guard pattern tightened to `/_(?:a11y|dev|test)$/u` with a test rejecting bare `dev`/`test`/`a11y` names. P4-F03: apps/web/README.md documents podman-based disposable database provisioning, migration, and the local test:a11y invocation. P4-F04: apply-steps.tsx second h1 -> h2. P4-F05: TextInputField Input now sets aria-required. P4-F06: review-moving-cards.tsx replaced by review-grid.tsx (ReviewGrid); stale pause tip removed from reviews.tsx. P4-F07: article editor save resolves via pure resolveArticleFormAfterSave (article-editor-state.ts) keeping edits typed during a pending save; two new tests. P4-F08: syncRolePermissions uses onConflictDoUpdate to persist scope changes. P4-F09: @fesk/db/runtime exposes SqlClient via Layer.provideMerge and exports withTransaction; gallery insert batched (insertArticleGalleryImages); article create/update mutations wrapped in transactions; @effect/sql@^0.51.1 added to packages/db via bun add. P4-F10: deleteUserRecord wraps audit + delete in one transaction, cascade FKs handle user_roles/sessions. P4-F11: runSops failure message is null-safe via result.error. P4-F12..F15: identity Effect.map removed; carousel reInit listener removed in cleanup; form-field contexts default to null with guards before use; pricing create effects die loudly on missing insert row before auditing. P4-F16: @next/env aligned to ^16.2.6 via bun add. P4-F17: shared a11y/test-base-url.ts; playwright.config.ts derives HOSTNAME/PORT from it; admin-fixtures.ts imports it.
- 2026-06-12 14:47:06 +0200 — Stale-reference sweep after pass-4 fixes: no remaining references to review-moving-cards/ReviewMovingCards; README "port 3100" statements match a11y/test-base-url.ts. Gate attempt 12 started: `rm -rf apps/web/.next && A11Y_DATABASE_SEEDING=1 DATABASE_URL=postgresql://fesk_web:fesk_web@127.0.0.1:5432/fesk_web_dev bun run check:fix`.
- 2026-06-12 14:52:30 +0200 — Gate attempt 12 failed: `noExcessiveCognitiveComplexity` (19 > 15) in updateArticleRecord's new transaction generator (article-write-effects.ts:79) — the four conditional-spread ternaries moved inside the generator pushed it over the limit. Fixed by extracting a pure `buildArticleUpdateSet(input)` helper; workspace lint and check-types verified green. Gate attempt 13 started: `rm -rf apps/web/.next && A11Y_DATABASE_SEEDING=1 DATABASE_URL=... bun run check:fix`.
- 2026-06-12 14:49:29 +0200 — Gate attempt 13 passed: 17/17 Turbo tasks successful, 129 web unit tests, 114 Playwright/Axe checks. Pass 5 started: convergence review of the pass-4 fix delta (transaction infra in @fesk/db, admin write effects, auth redirect fix, a11y/docs/test changes) with the full current diff as context. Reviewer invocations: architecture/types/database lens + unscoped convergence reviewer. Reviewers instructed not to edit files and not to re-run the full gate.
- 2026-06-12 15:07:14 +0200 — Pass 5 reviewer invocations complete: architecture/types/database lens (1), unscoped convergence reviewer (1). Retries: 0. Both independently verified the transaction mechanics (savepoint semantics confirmed in installed @effect/sql 0.51.1 internals, Layer.provideMerge shares one SqlClient instance, FK cascades confirmed in migration SQL, onConflictDoUpdate target matches the composite PK). Findings synthesized below; the two top findings were reported by both reviewers and deduplicated.
- 2026-06-12 15:07:14 +0200 — P5-F01 blocking VALID (fixed): new review-grid.tsx and modified review-list.tsx marked `className?` optional though the only call site (reviews.tsx) always passes it, violating the AGENTS.md optionality rule. Made required in both.
- 2026-06-12 15:07:14 +0200 — P5-F02 non-blocking VALID, both reviewers (fixed): transactionality was applied inconsistently — createUserRecord (insert + role-assignment loop), updateUserRecord (update + role sync + session revocation), and updateRoleRecord (update + permission upsert loop + prune delete) remained multi-statement and non-atomic in the same files pass 4 touched. Fixed: all three now wrap their writes in withTransaction; assignUserRoles batches the role insert (mirroring the gallery fix); syncRolePermissions batches the permission upsert with `excluded.scope`; extracted buildRoleUpdateSet to stay under the cognitive-complexity lint; removed the now-redundant `as JsonObject | null` cast; role-write-effects.test.ts provides a fail-on-access SqlClient proxy; @effect/sql@^0.51.1 added to apps/web devDependencies via bun add (test imports the tag directly).
- 2026-06-12 15:07:14 +0200 — P5-F03 non-blocking VALID, both reviewers (fixed): deleteUserRecord read the target email outside the transaction and the delete had no row confirmation, so a concurrent delete between check and transaction would commit a false `users.delete/success` audit row. Fixed: the existence check moved inside the transaction as SELECT ... FOR UPDATE (locks the row, NotFoundError if gone), keeping the audit-before-delete order required for self-deletion FK semantics.
- 2026-06-12 15:07:14 +0200 — P5-N07 nit VALID (fixed): the `pgDrizzleServiceProperty` computed-key indirection in packages/db/src/runtime.ts turned out to be a silent evasion of Biome `useNamingConvention`; replaced with the literal key plus a scoped biome-ignore with a stated reason, per AGENTS.md suppression policy. P5-N08 nit VALID (fixed): the upload queue's done state displayed untrimmed alt while the trimmed value was uploaded; markItemUploading now normalizes the stored alt, with a test assertion added.
- 2026-06-12 15:07:14 +0200 — DISCARDED P5-D01: "move ensureReferencedArticleMedia inside the article transaction" — the suggested move does not close the race (a plain SELECT takes no row lock), the media FK constraint already guarantees integrity, and the torn case is a narrow concurrent-delete race surfacing as a defect; mapping the FK SqlError to a 400 is speculative refinement.
- 2026-06-12 15:07:14 +0200 — DISCARDED P5-D02 (needs clarification): "add a live-database integration test for withTransaction rollback" — valid gap, but introducing a database-backed integration test layer into the bun unit-test run is a test-infrastructure/architecture decision per AGENTS.md ("ask before broad … workflow decisions"). Recorded as a verification gap for the final report instead.
- 2026-06-12 15:07:14 +0200 — DISCARDED P5-D03: per-invocation connection pool in runDatabaseEffect — pre-existing pattern explicitly flagged by both reviewers as not a regression; memoizing a ManagedRuntime changes server runtime lifecycle (HMR/serverless) and is out of loop scope.
- 2026-06-12 15:07:14 +0200 — DISCARDED P5-D04: audit-atomicity policy inconsistency (user delete audits inside the tx, articles audit post-commit) — deliberate: user deletion requires audit-before-delete inside the tx for self-deletion FK ordering; other mutations audit post-commit so an audit failure never rolls back a completed change. Policy recorded here rather than churning call sites.
- 2026-06-12 15:07:14 +0200 — DISCARDED P5-D05: merge review-grid/review-list into one layout-prop component — the components differ structurally (grid wraps cards in an inner grid div; list styles each card and takes parent layout), the duplication is ~8 lines, and merging risks visual regression on the landing page for no behavioral gain.
- 2026-06-12 15:07:14 +0200 — Local verification after pass-5 fixes: apps/web lint, check-types, and admin feature tests (28) green; packages/db lint and check-types green. Gate attempt 14 started: `rm -rf apps/web/.next && A11Y_DATABASE_SEEDING=1 DATABASE_URL=... bun run check:fix`.
- 2026-06-12 15:08:40 +0200 — Gate attempt 14 passed: 17/17 Turbo tasks successful, 130 web unit tests, 114 Playwright/Axe checks. Pass 6 started: convergence review of the pass-5 fix delta (transaction wrapping of user/role writes, FOR UPDATE TOCTOU fix, batched inserts, className required, biome-ignore with reason, alt trim normalization) — single unscoped convergence reviewer with the full current diff as context (delta is architecture-relevant).
- 2026-06-12 15:16:19 +0200 — Pass 6 result: one reviewer invocation, 0 retries. Reviewer verified the pass-5 transaction wrapping end-to-end (rollback on failure/defect/interrupt confirmed in installed @effect/sql source; audit joins the tx via the shared PgDrizzle tag; FOR UPDATE loser-path fails NotFoundError; batched upsert conflict targets match composite PKs; intra-statement duplicate hazard excluded by the uniqueness guards; test seam and devDependency correct). One finding. P6-F01 non-blocking VALID (fixed): updateRoleRecord's `updatedRole ?? existingRole` fallback masked a concurrent role deletion as success — false `roles.update` audit when permissions were undefined, raw SqlError instead of NotFoundError when defined; inconsistent with the user/article update paths. Fixed: fail NotFoundError when the in-tx update returns no row. Lint, check-types, and role-write tests green.
- 2026-06-12 15:25:38 +0200 — Gate attempt 15 failed: 1/114 a11y check failed — axe "list" violation (serious) on /angebot/vorschule, desktop only: a ul.list-disc directly contained a non-li child. Root cause investigated empirically (P6-F02, valid, environment/diff-adjacent): Next.js partial prerendering cuts the static shell mid-list (`<template id="P:2">` stands in for the second li, confirmed via curl of the standalone server); after `load`, React streams the postponed segment and briefly inserts a raw wrapper div into the ul before unwrapping it into the final li (caught with a MutationObserver-style polling probe: transient `li,div` children ~50ms wide, 16/20 concurrent loads). The a11y scan raced this window; reproduced 3/10, then 1/20 with a placeholder-absence wait alone. Fix in packages/a11y-testing/src/axe.ts: scanWcag22AaViolations now waits for all React streaming placeholders (template[id^="P:"], template[id^="B:"]) to resolve AND for structural DOM quiescence (no childList mutations for 250ms, 10s hard deadline) before running Axe. Verified: 30/30 repeated runs of the previously flaky route pass; packages/a11y-testing lint/types/tests green. This was a pre-existing latent flake in the shared scan helper, surfaced by gate repetition, not by the pass-5/6 delta. Gate attempt 16 started.
- 2026-06-12 15:26:42 +0200 — Gate attempt 16 passed: 17/17 Turbo tasks successful, 114 Playwright/Axe checks. Pass 7 started: convergence review of the pass-6 fix delta (updateRoleRecord NotFoundError on zero-row update; a11y scan helper streaming/quiescence wait) — single unscoped convergence reviewer; full diff available as context since the helper change affects every a11y spec.
- 2026-06-12 15:32:34 +0200 — Pass 7 result: one reviewer invocation, 0 retries. Both pass-6 changes verified correct (NotFoundError branch matches sibling paths and rolls back the permission sync; quiescence promise cannot hang or double-resolve; childList-only observer cannot be starved by the 6s/8s carousel autoplay; interaction-state specs unaffected). P7-F01 non-blocking DISCARDED (recorded reason): no unit test covers the new concurrent-deletion NotFoundError branch — consistent with the pass-4 decision against fake-Drizzle-chain tests, covering it requires the transactional PgDrizzle/SqlClient fake harness the test suite deliberately avoids; recorded as a verification gap spanning all three update paths (role, user, article zero-row branches). P7-N02 nit VALID (fixed): the 10s quiescence deadline previously resolved silently, so a future starved wait would resurface as a random Axe flake; waitForStreamedDom now returns whether the DOM quiesced and console.warns (scoped biome-ignore for noConsole with stated reason) when scanning proceeds past the deadline. packages/a11y-testing lint/types green; 10/10 repeated vorschule scans pass. Gate attempt 17 started.

- 2026-06-12 15:34:42 +0200 — Gate attempt 17 passed: `bun run check:fix` from repo root after `rm -rf apps/web/.next` — 17/17 Turbo tasks successful, 114/114 a11y checks passed. Pass-7 fix delta (quiescence deadline warning in packages/a11y-testing/src/axe.ts) verified green.
- 2026-06-12 15:34:42 +0200 — Pass 8 started: convergence review of the pass-7 fix delta. Delta: waitForStreamedDom now returns whether the DOM quiesced and console.warns (scoped biome-ignore for noConsole with stated reason) when scanning proceeds past the deadline. Self-contained local delta → single unscoped convergence reviewer, delta-only context.
- 2026-06-12 15:35:58 +0200 — Pass 8 completed: single unscoped convergence reviewer (delta-only context, pass-7 fix delta), 1 invocation, 0 retries, result "No findings." Loop converged clean. Final report produced from this ledger.
- 2026-06-12 15:40:22 +0200 — Ledger annotation (user request): segment headings added attributing passes 1–3 to GPT-5.5 xhigh and passes 4–8 to Claude Fable 5 high. No entry content changed.

