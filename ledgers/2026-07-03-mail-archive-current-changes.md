# Review loop: current changes

- Scope: current uncommitted workspace changes against `d59bb04`.
- User request: run a review loop against the current changes.
- Validation gate: `bun run check:fix`.

## Operations

- 2026-07-03 08:58:28 +0200: Deterministic gate `bun run check:fix` passed before reviewer pass 1.
- 2026-07-03 08:58:28 +0200: Opened ledger for review loop.

## Passes

- 2026-07-03 09:05:28 +0200: Pass 1 reviewed full current diff against `d59bb04`; fan-out lenses were security, accessibility, architecture/types, tests/docs, and catch-all. Reviewer invocations: 5. Retries: 0. Gate relied on: `bun run check:fix` passed.
- 2026-07-03 09:22:06 +0200: Worker fix pass completed. Parent reran `bun run check:fix`; passed. Worker reported `bun run db:generate` passed and `bun run db:migrate` failed because `DATABASE_URL` is not set. Parent confirmed `bun turbo run test:a11y --dry=json` fails because no `test:a11y` task exists in any workspace.
- 2026-07-03 09:28:23 +0200: Pass 2 reviewed full current diff after worker fix; fan-out lenses were security, accessibility, architecture/types, tests/docs, and catch-all. Reviewer invocations: 5. Retries: 0. Additional checks before/during pass: `bun run build` passed and `bun run db:generate` reported no schema changes.
- 2026-07-03 09:32:02 +0200: Worker fix pass 2 completed. Parent reran `bun run check:fix`; passed.
- 2026-07-03 09:38:17 +0200: Pass 3 final convergence reviewer inspected second-worker fix area. Reviewer invocations: 1. Retries: 0. Parent reran `bun run build`; passed.
- 2026-07-03 09:38:17 +0200: Worker fix pass 3 added DB secret example. Parent reran `bun run check:fix`; passed.
- 2026-07-03 09:39:52 +0200: Pass 4 reviewed DB docs/config delta. Reviewer invocations: 1. Retries: 0. Gate relied on: `bun run check:fix` passed after worker fix pass 3.
- 2026-07-03 09:41:40 +0200: Worker fix pass 4 updated DB README runtime `DATABASE_URL` documentation. Parent reran `bun run check:fix`; passed.
- 2026-07-03 09:43:27 +0200: Pass 5 reviewed DB docs/config delta and reported no findings. Reviewer invocations: 1. Retries: 0. Gate relied on: `bun run check:fix`, `bun run build`, and `bun run db:generate` passed.

## Findings

- RL-001 | 2026-07-03 09:05:28 +0200 | Blocking | security/catch-all/architecture | `packages/db/src/schema.ts`, `apps/worker/src/features/archive/archive-mail.ts`, `apps/worker/src/features/archive/storage.ts`, `packages/utils/src/s3.ts`, `packages/db/drizzle/0006_mailbox_kind.sql` | DB uniqueness and object storage keys are not scoped by account/mailbox, so sent/inbox or cross-account messages with the same `Message-ID` can collide. Disposition: fixed by scoping uniqueness to `(accountId, messageId, mailbox)`, adding scoped storage prefixes, and generating `0007_flashy_tinkerer.sql`. Verification: `bun run check:fix` passed; migration execution not run because `DATABASE_URL` is unset.
- RL-002 | 2026-07-03 09:05:28 +0200 | Blocking | architecture | `apps/ui/src/features/mail-list/mailbox-view.tsx`, `apps/ui/src/features/mail-list/index.ts` | `mail-list` feature imports sibling `mail` and `mail-search` features. Disposition: fixed by moving `MailboxView` to the route layer and removing the feature export. Verification: `bun run check:fix` passed.
- RL-003 | 2026-07-03 09:05:28 +0200 | Blocking | architecture | `apps/worker/src/features/sync/sent-mailbox.test.ts`, `apps/worker/package.json` | Worker tests import `bun:test` without a direct `@types/bun` dependency. Disposition: fixed by adding `@types/bun` to `@mail-archive/worker`. Verification: `bun run check:fix` passed.
- RL-004 | 2026-07-03 09:05:28 +0200 | Blocking | architecture | `apps/ui/src/app/(app)/[accountId]/sent/page.tsx` | New sent route adds forbidden Next route segment config. Disposition: fixed by removing `dynamic` from the sent page. Verification: `bun run check:fix` passed.
- RL-005 | 2026-07-03 09:05:28 +0200 | Blocking | architecture | touched code files over 200 lines | Several touched files exceed the repo line-count guideline without an exception document. Disposition: fixed by splitting changed files and adding a narrow Drizzle schema exception. Verification: `wc -l` shows touched files under 200 except `packages/db/src/schema.ts`, which is documented; `bun run check:fix` passed.
- RL-006 | 2026-07-03 09:05:28 +0200 | Blocking | database | `packages/db/package.json`, `package.json`, `turbo.json` | Drizzle commands are root-owned instead of DB workspace-owned Turbo wrappers. Disposition: fixed by adding DB-owned scripts and root Turbo wrappers/tasks. Verification: worker ran `bun run db:generate`; parent inspected scripts.
- RL-007 | 2026-07-03 09:05:28 +0200 | Blocking | accessibility | `apps/ui/src/features/layout/app-sidebar.tsx`, `apps/ui/src/shared/ui/presentation/components/sidebar.tsx` | Current mailbox nav state lacks `aria-current` and a non-color indicator. Disposition: fixed with `aria-current` and a visible marker in `AccountMailboxLinks`. Verification: `bun run check:fix` passed.
- RL-008 | 2026-07-03 09:05:28 +0200 | Non-Blocking | accessibility | `apps/ui/src/features/mail-search/mail-search.tsx` | Search input relies on placeholder text only. Disposition: fixed with an `sr-only` label tied to the input. Verification: `bun run check:fix` passed.
- RL-009 | 2026-07-03 09:05:28 +0200 | Nit | accessibility | `apps/ui/src/features/mail-list/mail-list.tsx` | Extra sent recipient count is exposed as terse `+N` text. Disposition: fixed with screen-reader text and unit coverage. Verification: `bun run check:fix` passed.
- RL-010 | 2026-07-03 09:05:28 +0200 | Non-Blocking | catch-all | `apps/ui/src/features/mail/actions.ts`, `apps/ui/src/features/mail-list/mail-list.tsx` | Sent search does not search the recipient displayed as the primary row counterpart. Disposition: fixed with a sent-mail `exists` recipient search helper and tests. Verification: `bun run check:fix` passed.
- RL-011 | 2026-07-03 09:05:28 +0200 | Blocking | tests/docs | `apps/worker/src/features/sync/sync-state.ts`, `apps/worker/src/features/archive/archive-mail.ts`, `apps/worker/src/features/digest/digest-scheduler.ts`, UI mailbox list/view files | Mailbox-specific cursor, blocklist, digest, and UI behavior need focused tests. Disposition: partially fixed with focused storage prefix/fallback, sent search, recipient display, and inbox-only blocklist tests. Sync-state and digest DB query behavior remain covered by type/gate inspection rather than live DB tests. Verification: `bun run check:fix` passed.
- RL-012 | 2026-07-03 09:05:28 +0200 | Blocking | docs | `README.md`, `apps/worker/README.md` | Root README still describes blocklist deletion without limiting it to inbox mail. Disposition: fixed by clarifying inbox-only deletion. Verification: `bun run check:fix` passed.
- RL-013 | 2026-07-03 09:05:28 +0200 | Blocking | accessibility | `apps/ui/package.json`, `package.json`, missing `packages/a11y-testing` | Full Playwright/Axe route coverage is absent. Disposition: discarded for this loop because it is a pre-existing platform-wide gap requiring auth/database/browser test infrastructure beyond the current mailbox change; concrete changed-UI accessibility defects remain tracked as RL-007 through RL-009.
- RL-014 | 2026-07-03 09:28:23 +0200 | Blocking | docs | `packages/db/package.json`, missing `packages/db/README.md` | DB workspace consumes `DATABASE_URL` through Drizzle commands but lacks required workspace configuration documentation. Disposition: fixed by adding `packages/db/README.md` documenting `DATABASE_URL`. Verification: `bun run check:fix` passed.
- RL-015 | 2026-07-03 09:28:23 +0200 | Blocking | accessibility | `apps/ui/src/features/layout/account-mailbox-links.tsx`, `apps/ui/src/features/layout/app-sidebar.tsx` | Repeated Inbox/Sent links are indistinguishable across accounts to assistive tech. Disposition: fixed by passing account name and adding explicit link `aria-label`s. Verification: `bun run check:fix` passed.
- RL-016 | 2026-07-03 09:28:23 +0200 | Blocking | tests/docs | UI mailbox DB queries, digest mail source, sync state, archive persistence | Review requested DB-backed or mocked tests for mailbox separation/persistence and digest exclusion. Disposition: discarded as a verification gap for this loop because no DB test harness exists and current behavior has focused pure-helper coverage plus passing type/lint/test/build gates; creating a DB harness is broader infrastructure work.
- RL-017 | 2026-07-03 09:28:23 +0200 | Blocking | architecture | worker feature boundary cycle among sync/archive/blocking | Reviewer identified a sibling-feature dependency cycle. Disposition: needs clarification/discarded for this loop because the main cycle predates this change (`sync -> archive` and `archive/blocking -> sync/errors` existed before); moving shared errors is small but consolidating feature ownership is broader architecture work outside the mailbox review loop.
- RL-018 | 2026-07-03 09:28:23 +0200 | Blocking | architecture | `apps/worker/src/features/digest/digest-mail-source.ts` | Digest DB/S3 helper exports raw async functions instead of typed Effect errors. Disposition: discarded for this loop because the file was split from existing async digest scheduler internals and does not introduce a new runtime boundary; broader Effect conversion should be handled as a dedicated architecture task.
- RL-019 | 2026-07-03 09:28:23 +0200 | Non-Blocking | types | `apps/worker/src/features/archive/storage.ts` | `SaveMailParams.mailbox` uses `string` instead of `MailboxKind`. Disposition: fixed by using `MailboxKind`. Verification: `bun run check:fix` passed.
- RL-020 | 2026-07-03 09:28:23 +0200 | Nit | types | `apps/ui/src/features/mail/services/mail-search-query.ts`, `apps/ui/src/features/mail/actions.ts` | Search helper returns `SQL | undefined` despite always returning a condition; caller has dead guard. Disposition: fixed by returning `SQL` and removing the dead guard. Verification: `bun run check:fix` passed.
- RL-021 | 2026-07-03 09:38:17 +0200 | Blocking | docs/config | `packages/db/README.md`, `packages/db/secrets/dev.example.yaml` | DB README documents `DATABASE_URL` as a secret without a matching example YAML shape. Disposition: fixed by adding `packages/db/secrets/dev.example.yaml` and referencing it from the README. Verification: `bun run check:fix` passed.
- RL-022 | 2026-07-03 09:39:52 +0200 | Blocking | docs/config | `packages/db/README.md`, `packages/db/src/db.ts` | DB README documents `DATABASE_URL` for Drizzle commands but not runtime `getDb`/`getPool`/`db` usage. Disposition: fixed by documenting runtime users, requiredness, default behavior, and throw-on-unset behavior. Verification: `bun run check:fix` passed.
